void main() {
  // basic type
  String name = "tw";
  bool alive = true;
  int age = 12;
  double mo = 69.9;
  // 변수 타입은 dart에서는 class로 정의되어 있음

  num n = 1; // num은 숫자에 관한 변수 타입
  n = 9.3; // int, double 형에 관계없이 받을 수 있음
}
